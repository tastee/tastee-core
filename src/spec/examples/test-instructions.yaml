log some $value twice:
    - console.log('log once '+$value+', and twice '+$value);

call go to google :
    - go to 'http://www.google.fr'

call go to $url:
    - go to $url

visit selenium wikipedia :
    - go to wikipedia.Selenium

empty instruction:

instruction with empty line:
    -

instruction containing empty line:
    - console.log('line 1')
    -
    - console.log('line 3')