test getting started:
    - page.goto('https://tastee.github.io').then( () => {
        page.waitForSelector('#download-button').then( () => {
          page.click('#download-button').then( () => {
            page.$eval('body > div:nth-child(9) > div > div > div > h2', e => e.innerHTML).then(html => {
               assert.equal(html, 'How to use Tastee ?', 'the element contains '+html);
             });
          });
        });
      });
